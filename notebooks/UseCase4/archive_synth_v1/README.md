# Archive — Synthetic Data Prototype (v1)

These notebooks were developed using **synthetic EPC data** before real TR Meram project data was available. They are kept for development history only.

**Not used in thesis results.** All thesis analysis uses the notebooks in the parent directory (`notebooks/UseCase4/01–08`), built on real TR Meram data.
